import React,{useEffect,useState}from'react';import{createRoot}from'react-dom/client';import{Play,Download,Settings}from'lucide-react';import'./style.css';
type Media={id:string;title:string;year:number;resolution:string;poster_url?:string;plot?:string};
function App(){const[items,setItems]=useState<Media[]>([]);const[q,setQ]=useState('');useEffect(()=>{fetch('/api/search?q='+encodeURIComponent(q),{headers:{authorization:'Bearer '+localStorage.token}}).then(r=>r.ok?r.json():[]).then(setItems)},[q]);return <main className="min-h-screen bg-slate-950 text-white"><nav className="p-4 border-b border-slate-800 flex justify-between"><b>Telegram Media Cloud</b><span><Settings/> Admin</span></nav><section className="p-6 grid gap-4"><input className="rounded bg-slate-900 p-3" placeholder="Search IMDb, TMDB, title, genre, actor, studio" onChange={e=>setQ(e.target.value)}/><div className="grid md:grid-cols-4 gap-4">{items.map(m=><article className="bg-slate-900 rounded-xl p-4" key={m.id}><h2 className="font-bold">{m.title} ({m.year})</h2><p>{m.resolution}</p><div className="flex gap-2 mt-3"><a className="btn" href={`/api/play/${m.id}`}><Play/>Play</a><a className="btn" href={`/api/download/${m.id}`}><Download/>Download</a></div></article>)}</div></section></main>}createRoot(document.getElementById('root')!).render(<App/>);
